const oceanVariant = { name: 'ocean', definition: '.ocean &' }
const ariaDeselected = { name: 'aria-deselected', definition: '&[aria-selected="false"]' }
const ariaEnabled = { name: 'aria-enabled', definition: '&[aria-disabled="false"]' }

const variants = [oceanVariant, ariaDeselected, ariaEnabled]

module.exports = variants
